export const APP_VERSION = process.env.NEXT_PUBLIC_APP_VERSION || "development";
export const SCHEMA_VERSION = 1; // Incremental integer for IndexedDB schema
export const CACHE_VERSION = "v1"; // Prefix for static caches
